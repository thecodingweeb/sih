[project]
name = "sih-backend"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "numpy",
    # Add any other modules your code imports, for example:
    # "scipy",
    # "pandas"
]

[tool.vercel]
entrypoint = "backend_server:app"
