[project]
name = "sih-backend"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic",
    "numpy",
    "pandas",
    "scipy",
    "requests",
    "httpx"
]

[tool.vercel]
entrypoint = "backend_server:app"
